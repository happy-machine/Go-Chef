<div class="list">
  <% @users.each do |user| %>
  <div class="chef-list">
    <%= link_to "#{user.name}", user_path(user) %> 
    <button class="button show"><%= link_to "Show", user_path(user)%> </button>

    
  </div>
  <% end %>
</div>
